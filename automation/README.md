# Automation

`automation/` is the root-level automation area for the repository.

`publish/` contains the interactive helpers used from the repository root to run
local development targets and publish the backend or a frontend project.
