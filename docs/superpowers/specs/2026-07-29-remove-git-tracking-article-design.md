# 删除 Git Tracking 文章设计

## 目标

从 Hub 中删除文章《一次错误的 Git Tracking：我为什么不再把方向盘完全交给 Coding Agent》，保留另外两篇首发文章。

## 实现

- 删除文章目录 `2026-07-29_100001_codex-git-steering-wheel/`。
- 保留文章 `100002` 与 `100003`，不修改正文或固定地址。
- 继续由现有构建流程扫描文章目录并生成文章注册表，不增加隐藏状态或额外配置。
- 将 `100001` 视为已废弃 ID，后续文章不得复用。
- 保留既有设计文档中的历史记录，不回写已经完成的设计。

## 结果

- 首页与文章列表只展示 `100003`、`100002`。
- `/hub/articles/100001` 进入现有的文章不存在状态。
- 生产构建生成 2 篇文章、0 张文章图片。

## 验证

- 验证开发与生产文章注册表仅包含 `100003`、`100002`。
- 运行根目录测试、前端测试、类型检查、lint 与 Hub 生产构建。
- 发布后使用真实浏览器检查首页、文章列表、两个保留的详情页和 `100001` 不存在状态。
